Тестовое задание для BlackWallGroup


1) Сделано по шаблону Producer - > Consumer, при помощи брокера сообщений RabbitMQ

Концепция приложения:

1) запускается RabbitMQ
2) запускается FASTAPI
3) из папки app запускается несколько экземляров скрипта main.py (имитирующих клиентов)
для клиентов создаются отдельные очереди
запросы обрабатываются при помощи FASTAPI
бд на postgresql, схема с клиентами и их балансами


Приложение написано плохо(по моему мнению), безусловно его можно отрефакторить, убрать повторения, добавить аутентификацию юзеров,
более красиво прописать сами операции,схему БД, а главное продумать получше взаимосвязь объектов, однако это все опускалось по причине
дедлайнов.

RabbitMQ был изучен с нуля при решении данного задания
Postgresql был установлен для решения этого задания и изучен синтаксис

к сожалению упаковать все в Docker не успел.